import React from 'react';

export const TransactionFilters = ({ filters, setFilters, accounts, areas, isFiltersVisible, setIsFiltersVisible }: any) => {
    return isFiltersVisible ? <div className="p-4 border">Filters Placeholder</div> : null;
};
