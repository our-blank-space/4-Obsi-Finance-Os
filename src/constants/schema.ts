// src/constants/schema.ts

/**
 * Versión oficial del esquema de datos del plugin.
 *
 * Regla:
 * - Esta constante es la ÚNICA autoridad sobre la versión actual.
 * - Nunca se reduce.
 * - Cada incremento implica una migración nueva.
 */
export const SCHEMA_VERSION = 7; 
